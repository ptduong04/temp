apiVersion: v1
kind: Pod
metadata:
  name: reject-latest
  namespace: demo
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
