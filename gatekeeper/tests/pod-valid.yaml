apiVersion: v1
kind: Pod
metadata:
  name: pass-valid-pod
  namespace: demo
spec:
  containers:
  - name: nginx
    image: nginx:1.27
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
