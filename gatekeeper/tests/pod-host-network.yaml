apiVersion: v1
kind: Pod
metadata:
  name: reject-host-network
  namespace: demo
spec:
  hostNetwork: true
  containers:
  - name: nginx
    image: nginx:1.27
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
